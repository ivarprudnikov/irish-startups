import { Routes } from '@angular/router';
import { Component } from '@angular/core';

@Component({
  selector: 'main',
  template: `<p>Main component loaded</p>`
})
export class Main {
  constructor(){
  }
}
